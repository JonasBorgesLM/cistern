# SSH keys allowed to sign release tags, checked by the `release` job in
# .github/workflows/release.yml (RNF-10).
#
# The copy on `main` is the one the release job reads; a key added anywhere
# else signs nothing until it is reviewed into main (#115). With no key, every
# release fails: an unsigned release is one nobody can attribute, and a check
# that warns instead of failing gates nothing.
#
# To add a key, from the machine that will cut tags:
#
#   ssh-add -L                      # or: cat ~/.ssh/id_ed25519.pub
#
# then add one line per key, in the format git expects:
#
#   <email> namespaces="git" ssh-ed25519 <key>...
#
# and configure git to sign tags with it:
#
#   git config --global gpg.format ssh
#   git config --global user.signingkey ~/.ssh/id_ed25519.pub
#   git config --global tag.gpgsign true
#
# See RELEASING.md, "Before the first tag of a module".

jonasleo92@yahoo.com.br namespaces="git" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAoMtarMawZsFsEu/l5bi9M2zVYoGWEfzVAhk5TkXIN1
